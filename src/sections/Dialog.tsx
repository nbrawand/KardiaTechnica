import Page from '../components/Page'

function Dialog() {
  return (
    <Page>
      <h2>Dialog</h2>
      <p>
        A place to read and navigate articles. Each article will become its own
        page.
      </p>
      <p>
        Placeholder — same index + detail pattern as technology, sorted
        newest-first once content is added.
      </p>
    </Page>
  )
}

export default Dialog
